from django.apps import AppConfig

class EstoqueConfig(AppConfig):
    name = 'estoque'  # Nome do aplicativo Django
    default_auto_field = 'django.db.models.BigAutoField'  # Define o tipo de campo primário padrão para os modelos do aplicativo
